# Local Postgres runs in a Docker container: npm run db:up
DATABASE_URL=""

# Auth.js — generate a secret with: openssl rand -base64 32
AUTH_SECRET=""
# Only needed in production / non-localhost (localhost is covered by trustHost)
AUTH_URL="https://your-domain.example"

# Google OAuth client — see README "Google sign-in setup"
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""
